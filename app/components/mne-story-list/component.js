import Ember from 'ember';

// mne-story-list takes in:
// model: should be an array of objects
// source: defaults to 'the-next-web'
// title: the title of the well (optional)
export default Ember.Component.extend({});
